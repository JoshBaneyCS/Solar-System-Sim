[package]
name = "render_core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "render_core"

[dependencies]
wgpu = "24"
bytemuck = { version = "1", features = ["derive"] }
pollster = "0.4"
log = "0.4"
